import 'package:flutter/material.dart';

class BanSanPhamScreen extends StatelessWidget {
  const BanSanPhamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bán Sản Phẩm')),
      body: const Center(child: Text('Màn hình Bán Sản Phẩm')),
    );
  }
}
